dist: bionic

language: python

python:
  - "3.6"

install:
  - pip install -r requirements.txt

services:
  - redis-server

before_script:
  - export PYTHONPATH=$PYTHONPATH:$(pwd)
  - export DATA_ROOT=/tmp/codebook_automation/

script:
  - pytest
